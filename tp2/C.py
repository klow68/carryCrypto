#! /usr/bin/python
#-*-coding: utf-8 -*-

import random
import sys
import math


def miller_rabin(n,k = 10):
	#n=2**s*d trouver s et d
	s=?
	d=?
	for i in range(k):
		r = random.randint(2,n-2)
		x = a**d%n
		if x==1 or x==-1:
			for r in range(1,s-1):


